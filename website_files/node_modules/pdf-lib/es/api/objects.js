import {
    PDFName,
    PDFNumber
} from "../core";
export var asPDFName = function asPDFName(name) {
    return name instanceof PDFName ? name : PDFName.of(name);
};
export var asPDFNumber = function asPDFNumber(num) {
    return num instanceof PDFNumber ? num : PDFNumber.of(num);
};
export var asNumber = function asNumber(num) {
    return num instanceof PDFNumber ? num.asNumber() : num;
};